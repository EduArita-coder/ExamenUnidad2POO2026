info:
  name: Delete
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{PARH_URL}}/empleados/1112"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
