info:
  name: GetActivo
  type: http
  seq: 6

http:
  method: GET
  url: "{{PARH_URL}}/empleados/activos"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
