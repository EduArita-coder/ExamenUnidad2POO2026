info:
  name: GetPorPeriodo
  type: http
  seq: 8

http:
  method: GET
  url: "{{PARH_URL}}/planillas/periodo/2024"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
