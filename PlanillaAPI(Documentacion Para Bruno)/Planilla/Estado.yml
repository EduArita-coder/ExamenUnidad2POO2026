info:
  name: "Estado "
  type: http
  seq: 6

http:
  method: PUT
  url: "{{PARH_URL}}/planillas//estado"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
