info:
  name: GetOne
  type: http
  seq: 2

http:
  method: GET
  url: "{{PARH_URL}}/detallesplanilla/0001"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
